spring:
  servlet:
    multipart:
      max-file-size: 30MB
      max-request-size: 30MB
  mail:
    host: 172.28.142.5
    port: 25
  thymeleaf:
    check-template-location: true
    prefix: classpath:/templates/
    suffix: .html
    mode: HTML5
    encoding: UTF-8
    cache: false
  jpa:
    generate-ddl: true
    database: POSTGRESQL
  config:
    import: optional:prod.yml
  #main:
  #  allow-circular-references: true

server-config:
  upload-path: '/var/lib/docker/volumes/psg-volume/_data'

encoderKey: "20002302031022BZ"

#172.28.142.5

#172.28.142.101 - for local smtp